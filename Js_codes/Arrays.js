let numbers = new Array(1,2,3,4,5,6,7,8,9);
console.log(numbers.splice(0,5,13));

const list = ['foo','bar','bar'];
const uniq = new Set(list);
const dict = new Map([
    ['foo',1],
    ['bar',2],
]);